// $ dotnet run -- 4S-4T+3T-CCSp4-3Ax-FSSp4-StSq3-CCoSp4
// エレメントは `-` で区切る
// 連続ジャンプは `+` で区切る
// 後半ジャンプは末尾に `x` を付与する
// -t single/pair/icedance で種目を判別(デフォルトはsingle)

Console.WriteLine("Hello, World!");
